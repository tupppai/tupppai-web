define(['app/controllers/index'],
    function (index) {
        "use strict";

        return {
            index: index,
            logAction: function (action) {
                //MyApp.content.show(new LandingView());
            }
        };
    });
