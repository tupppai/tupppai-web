define(['underscore', 'app/views/CommendView'],
    function (_,  CommendDetailView) {
        "use strict";

        return function(id) {

            var view = new CommendDetailView({});
        };
    });