define(['underscore', 'app/views/HomeView'],
    function (_,  HomeView) {
        "use strict";

        return function() {

            var view = new HomeView({
            	
            });
        };
    });
